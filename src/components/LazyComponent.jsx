import React from "react";

const LazyComponent = () => {
  return (
    <div style={{ padding: "1rem", fontSize: "1.25rem", textAlign: "center" }}>
      👋 This is a lazy-loaded component!
    </div>
  );
};

export default LazyComponent;
